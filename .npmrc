# Default to blocking lifecycle scripts for safer local installs.
# Workflows that need vetted postinstall hooks (for example @smogon/calc from
# smogon/damage-calc) must opt in per command with --ignore-scripts=false.
ignore-scripts=true
